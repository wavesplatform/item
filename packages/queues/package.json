{
  "name": "@item/queues",
  "version": "1.0.0",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "fix": "run-s fix:tslint fix:prettier",
    "fix:prettier": "prettier --write \"{src,test}/**/*.ts\" --ignore-path ../../.prettierignore",
    "fix:tslint": "tslint --fix -t stylish -p .",
    "test": "run-s test:jest",
    "test:jest": "jest --passWithNoTests --config=jest.json",
    "test:watch": "jest --passWithNoTests --config=jest.json --watch"
  },
  "dependencies": {
    "bull": "^3.10.0"
  },
  "devDependencies": {
    "@item/types": "^1.0.0",
    "@types/bull": "^3.10.2",
    "@types/node": "^12.7.2",
    "@waves/waves-rest": "^0.0.36",
    "npm-run-all": "^4.1.5",
    "prettier": "^1.18.2",
    "ts-node": "^8.3.0",
    "tslint": "^5.19.0",
    "typescript": "^3.5.3"
  }
}
