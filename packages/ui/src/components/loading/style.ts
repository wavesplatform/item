import { Flex, FlexProps } from 'rebass'
import styled from '@emotion/styled'

export type StyledLoadingProps = FlexProps

export const StyledLoading = styled(Flex)<StyledLoadingProps>`
  margin: 0 auto;
`
